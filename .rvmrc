export RUBYOPT="rubygems"
export RUBYLIB="."
rvm ruby-1.9.2@samurai-sinatra-demo

# Install bundler if it doesn't already exist
# if ! command -v bundle > /dev/null ; then
#  gem install bundler
# fi
